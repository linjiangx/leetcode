#笨办法，穷举
class Solution:
    def twoSum(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """
        len1=len(nums)
        for i in range(len1-1):
            for j in range(i+1,len1):
                if (nums[i]+nums[j])==target:
                    return [i,j]
