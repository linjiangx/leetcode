#从前往后赋值时，小心len=5,k=2之类的情况，第五个数会使用第三个数，而第三个数已经使用了第一个数，这样第五个数变成了第一个数，出错
class Solution:
    def rotate(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: void Do not return anything, modify nums in-place instead.
        """
        len1 = len(nums)
        k = k % len1
        num1=[]
        for i in range(k):
            num1.append(nums[i+len1-k])
        for i in range(len1-k-1,-1,-1):
            nums[i+k]=nums[i]
        for i in range(k):
            nums[i]=num1[i]

这个方法更简洁，将最后面的值插入前面
for i in range(k):
            nums.insert(0,nums.pop())
